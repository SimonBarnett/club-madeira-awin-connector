# Merc hostile MRB — club-madeira-awin-connector (2026-09-19)

**Repo:** https://github.com/SimonBarnett/club-madeira-awin-connector  
**Reviewed tip:** `0666b74978dcb70cd5cefdda7c076ea6d24b16d6` (`0666b74`, immediately after P5 `a3ec1c7`)  
**Bob engineering MRB:** [docs/mrb-awin-p5-publisher-injection-2026-09-19.md](./mrb-awin-p5-publisher-injection-2026-09-19.md) — PASS, ready for Merc  
**Lab evidence:** [docs/mrb-merc-evidence-2026-09-19.md](./mrb-merc-evidence-2026-09-19.md) (PR #1)  
**Skill:** merc-hostile-mrb  

## Verdict

**PASS-with-nits** (Merc test)

Publisher-agnostic P5 exit criteria hold in code and offline tests. Dry-run default and Join stub rules hold. **Not** Join-shipped. **Not** final product UAT. **Not** live `api.awin.com`. Merc may use this connector for **read / dry-run / queue** automation once a publisher id and token are supplied; **do not** treat the worker as live Join.

Bob: nits below are **docs-only** (no product code rework required). No new build agent needed for src/. Park this review under `/docs` and merge evidence PR when ready.

## Pass bar checklist

| Bar | Result |
|---|---|
| Publisher id runtime (`--publisherId` / `AWIN_PUBLISHER_ID`) | **PASS** — missing → exit 1 / `MissingPublisherIdError` |
| No `2889699` in `src/` | **PASS** — `rg` 0 hits; T-ID walk test |
| Dry-run default true | **PASS** — `DEFAULT_DRY_RUN`; CLI envelope `dryRun: true` |
| No invented join write API | **PASS** — `JOIN_WRITE_API === null` |
| Worker `needs_auth` | **PASS** — stub; non-dry-run `applyJoin` → `needs_auth` |
| Per-publisher queue persist | **PASS** — `data/join-queue/{id}.json`; T-ID-03 |
| Per-client / per-token rate limit | **PASS** — `processLimiter` not production default (T-ID) |
| Truncation / lastSeen honesty | **PASS** — envelope `truncated`; omit missing lastSeen |
| Second publisher via CLI | **PASS** — exercised `9999999` |
| `npm test` / typecheck | **PASS** — 29/29; `tsc --noEmit` clean |
| Secrets in repo | **PASS** — no token material in evidence tree |

## Evidence (what Merc ran / saw)

- Cloud lab on tip `0666b74`: vitest 29 passed (6 files), typecheck clean.
- CLI `list_join_candidates --publisherId 9999999 --fixture …` → `publisherId: 9999999`, `dryRun: true`, exit 0.
- Missing publisher id → stderr + exit 1.
- Source review: `src/publisher.ts`, `src/config.ts`, `src/apply.ts`, `src/worker/stub.ts`, `src/queue.ts`, `src/cli.ts`, `tests/t-id.test.ts` match Bob’s closed NC-01…NC-05.
- **Not run:** live Join, Playwright T-UI-05, real `AWIN_ACCESS_TOKEN`, live `api.awin.com`, Phase 0 modal screenshot, U1–U8 operator gates.

## Nits (ordered required fixes — docs)

1. **Scrub `docs/build-and-test-plan.md` §3 locked-constants sample.** It still shows `export const PUBLISHER_ID = 2889699` and hardcodes `/publishers/2889699` in the endpoint table. That will re-poison the next build agent. Align with P5: injected id only; Madeira as example tenant in prose/examples, never as a compiled default recipe.
2. **Reconcile `docs/functional-spec.md` “LOCKED Publisher 2889699” with P5 addendum.** Either mark Madeira as example tenant for agent hand-off, or split “Club Madeira deployment constants” from “product identity” so agents do not recompile a default.
3. **Package/repo name `club-madeira-awin-connector`** remains historical (Bob already non-blocking). Optional rename later; not a Merc FAIL.

## Blockers

None for Merc test of P5 / dry-run slice.

## Explicit non-claims

- Not ready for shipping Join / Playwright apply.
- Not Merc approval of T-UI-05.
- Not replacement of Welcome IMAP / 8am onboarding / partner Add Key.
- Stub `needs_auth` is not a successful join.

## Ordered fixes for Bob

1. Doc PR: rewrite build-and-test-plan §3 + endpoint examples to injected `$PUBLISHER_ID` (required).
2. Doc PR: clarify functional-spec LOCKED publisher vs P5 tenant injection (required for agent safety).
3. Optional: package rename / help banner de-branding.

## Next

- Bob: park this file (+ PDF if attached) under `docs/`; merge evidence PR #1 or fold docs.
- Simon: Merc test **PASS-with-nits**. Connector is usable for offline/dry-run with injected publisher id. Join remains gated on U1/U2/U6 + T-UI-05.
- Merc standing by for next tip if Bob opens a docs-only fix PR; no code re-MRB required unless src/ changes.

## Sign-off

Merc hostile test · 2026-09-19 Europe/London · tip `0666b74`
