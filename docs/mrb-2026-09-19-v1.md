# Hostile MRB — club-madeira-awin-connector (2026-09-19 v1)

**Job after:** `d11ead07`  
**Range:** `a16ea2c..dc25deb`  
**HEAD:** `dc25debfba75a6bae2d56f241ad7d974243d23f8`  
**Requested host:** IONOS `293361b2-5bf0-4931-a825-6145d1cace70` path `C:\ai\club-madeira-awin-connector`  
**Evidence host:** box re-run at same SHA (this executor is box-scoped; `Shell`+`machineId` is not available here). Tip matches `origin/main`.

## Verdict

**PASS-with-nits for first ticket (P0–P4).**  
**FAIL for human UAT / Join / T-UI-05** — by design. Phase 0 and U-gates are operator. Do not ship as Join-ready.

Bob agrees with the README: **not ready for human UAT.** The build did the right thing: dry-run default true, no Join click, no invented write API, worker stub `needs_auth`.

Sub-verdict on the *first-ticket DoD* in `docs/build-and-test-plan.md` §10: **met**, with nits below.

---

## What landed (credit)

| Phase | Evidence |
|---|---|
| P0 | scaffold, vitest, CI `npm test` + `typecheck` |
| P1 | `AwinClient` GET publishers/programmes/programmedetails; POST `/publisher/{id}/promotions`; Bearer auth; 18/min wait policy; redaction |
| P2 | `criteria.ts` 99 / ProductFeed / 90d / 150; `JoinQueue` + `applyJoin` dry-run; CLI `list_join_candidates` / `relationship_status` |
| P3 | `runJoinWorker` → `{ status: 'needs_auth' }`; dry-run false path blocks UI |
| P4 | README locked constants, Phase 0 curls, AM email (U7), U1–U8 checklist |

Commits in range:

1. `e3ac8c0` chore: P0 scaffold TypeScript, vitest, and CI  
2. `8b036bf` feat: P1-P3 Awin read client, criteria, dry-run queue, worker stub  
3. `dc25deb` docs: P4 README locked constants, Phase 0, U-gates  

Diffstat vs `a16ea2c`: **+3191 / −20 across 24 files** (implementation commits).

---

## Blockers

### For Join / human UAT (operator — not code defects)

1. **Phase 0 not executed** — no curl lengths, no headed Join-modal screenshot (Cancel only). U1/U2 still open.  
2. **U6 worker host unset** — must not be walrus / IONOS media-host; overnight Chrome profile host unnamed.  
3. **T-UI-05 gated** — no Simon `advertiserId`; stub correctly refuses. Do not enable Playwright Join.  
4. **U7** — join write API remains closed until AM written yes. Correct; do not invent.

### For first-ticket code DoD

**None hard.** Tests map to T-SEL / T-API / T-APP / T-RED / T-RATE IDs. Default dry-run on. No Join invent. Stub `needs_auth`.

---

## Nits (non-blocking for first ticket; fix before leaving dry-run)

1. **`JoinQueue` is process-local `Map`** — plan §5 says Persist candidates + state. Caps and `dry_run_done` reset on process exit. Acceptable for dry-run scaffold; **must harden before live applies**. `.gitignore` already reserves `data/`.  
2. **`CRON_BATCH` / `INTERACTIVE_BATCH_MIN` unused** — only `INTERACTIVE_BATCH_MAX` (12) used in live `list_join_candidates`; silent truncate of notjoined list. Document or surface count.  
3. **`PROMOTION_URL` locked but unused** — fine until U2; keep unused rather than guessing a modal field.  
4. **`RateLimiter` not concurrency-safe** — fine for single CLI process; document if multi-worker.  
5. **No CLI smoke for `--fixture` in CI** — unit coverage exists via `rankJoinCandidates`; optional e2e nit.  
6. **IONOS evidence gap this MRB** — re-run `npm test` + `typecheck` on IONOS at `dc25deb` if Bob wants host-parity ink (expected identical).

---

## Locked-constant / safety audit (brutal)

| Check | Result |
|---|---|
| Publisher `2889699` | locked in `config.ts` |
| Client budget 18/min | `CLIENT_CALLS_PER_MIN`; T-RATE-01/02 |
| Message max 150 (not 170) | `MESSAGE_MAX`; T-SEL-03/04 |
| Dry-run default true | `DEFAULT_DRY_RUN` / `isJoinDryRun`; T-APP-01 |
| No join write API | `JOIN_WRITE_API = null`; promotions POST is offers only; URL has no `/join` |
| No Playwright Join | stub only; apply dry-run false → `needs_auth` |
| Secrets | `.env` gitignored; `.env.example` empty token; no secrets invented or committed |
| CI live Awin | mocked fetch only |

---

## Evidence

| Check | Result |
|---|---|
| `git pull --ff-only` | Already up to date at `dc25deb` |
| `npm test` | **21 passed** (5 files): t-sel 6, t-api 5, t-app 5, t-red 3, t-rate 2 |
| `npm run typecheck` | **exit 0** |
| Spec review | `docs/functional-spec.md` + `docs/build-and-test-plan.md` P0–P4 |
| Phase 0 / U-gates | operator — open |
| Join | not performed |
| GitHub Actions | run 35457042170 success on `dc25deb` (peer evidence) |

**Bob:** first ticket may land. Do **not** declare UAT. Operator owns Phase 0 + U1/U2/U6 before any T-UI-05 ticket. Next build: durable queue under `data/` (or explicit memory-only until T-UI-05 note in README), surface truncated candidate counts on CLI.
