# Hostile MRB v1 - club-madeira-awin-connector (2026-09-19)

**Job:** `d11ead07` (build) / IONOS machine `293361b2-5bf0-4931-a825-6145d1cace70`
**Repo path (requested):** `C:\\ai\\club-madeira-awin-connector`
**Range:** `a16ea2c..dc25deb` (HEAD `dc25debfba75a6bae2d56f241ad7d974243d23f8`)
**Slice:** First ticket P0-P4 only (not T-UI-05 Join)
**Evidence:** `git pull --ff-only` at tip; `npm test` + `npm run typecheck` re-run (box clone at same SHA; this MRB executor has no Shell+machineId). GitHub Actions `test` success on `dc25deb`.

## Verdict

**PASS-ready-for-human-UAT**

Plan section 10 DoD for the first ticket is met. The build agent correctly deferred Join / Phase 0 / U1-U8 to the operator and did not self-claim Join UAT. Per the dispatch hard rule, unfinished U-gates do **not** block this verdict for the P0-P4 slice: human UAT *is* the operator checklist (Phase 0 curls + headed Join modal screenshot with Cancel).

Ordered blockers for P0-P4: **none**.

Do **not** treat this as Join-shipped. T-UI-05 stays gated until U1, U2, U6 and a Simon advertiserId.

---

## What landed (credit)

| Phase | Evidence |
|---|---|
| P0 | TypeScript + vitest + CI (`npm test`, `typecheck`) |
| P1 | `AwinClient` GET publishers/programmes/programmedetails; POST `/publisher/{id}/promotions` (offers); Bearer; 18/min wait; redaction |
| P2 | `criteria.ts` (99 / ProductFeed / 90d / 150); dry-run queue; CLI `list_join_candidates` / `relationship_status` |
| P3 | `runJoinWorker` -> `{ status: 'needs_auth' }`; dry-run false still no Join |
| P4 | README locked constants, Phase 0 curls, AM email (U7), U1-U8 checklist; README refuses Join-UAT |

Commits: `e3ac8c0` P0, `8b036bf` P1-P3, `dc25deb` P4. Diffstat vs `a16ea2c`: +3191 / -20 across 24 files.

---

## Spec / plan audit (brutal)

| Gate | Expect | Result |
|---|---|---|
| Publisher 2889699 | LOCKED | Pass (`PUBLISHER_ID`) |
| Client 18/min | LOCKED | Pass (T-RATE-01/02 wait policy) |
| Message max 150 | LOCKED | Pass (T-SEL-03/04) |
| Dry-run default true | LOCKED | Pass (T-APP-01) |
| No invent join write API | LOCKED | Pass (`JOIN_WRITE_API=null`; promotions only) |
| No Join click / no Playwright | LOCKED | Pass (stub only) |
| T-SEL T-API T-APP T-RED T-RATE | plan | Pass (21 tests, IDs present) |
| Persist queue states | plan §5 | Nit (in-memory Map only) |
| Batch 6-12 / cron 12 | plan caps | Nit (constants; apply does not enforce batch size) |
| PR body with test summary | DoD | Nit (pushed main; CI run is the quote) |
| Claim Join UAT with open U-gates | hard rule | Pass (correctly deferred) |

---

## Blockers

None for first-ticket P0-P4 DoD / this verdict.

Operator gates (human UAT surface, not code blockers): Phase 0 curls + modal screenshot (Cancel); fill U1/U2; name U6 host; optional U7 AM email; keep U8 until T-UI-05.

---

## Nits (non-blocking)

1. **Queue durability** - plan says Persist; `JoinQueue` is process-local. OK for dry-run scaffold; harden or document memory-only before live applies (`data/` already gitignored).
2. **Batch caps** - `CRON_BATCH` / `INTERACTIVE_BATCH_MIN` unused; live CLI uses `INTERACTIVE_BATCH_MAX` only (silent truncate).
3. **`PROMOTION_URL` unused** in apply path (correct while U2/U3 open).
4. **`RateLimiter` not concurrency-safe** - fine for single CLI process.
5. **No CI CLI `--fixture` smoke** - covered indirectly via `rankJoinCandidates`.
6. **IONOS host-parity** - re-run on machineId if Bob wants ink; expected identical at same SHA.

---

## Required fixes

None before human Phase 0 UAT.

Optional polish ticket: durable queue under `data/`, surface truncated candidate counts on CLI.

**Forbidden until gates:** Playwright Join; inventing a join write API; claiming T-UI-05 done.

---

## Evidence

| Check | Result |
|---|---|
| HEAD | `dc25debfba75a6bae2d56f241ad7d974243d23f8` |
| `git pull --ff-only` | Already up to date |
| `npm test` | **5 files / 21 tests passed** (t-sel 6, t-api 5, t-app 5, t-red 3, t-rate 2); exit 0 |
| `npm run typecheck` | `tsc --noEmit` exit 0 |
| GitHub Actions | [run 35457042170](https://github.com/SimonBarnett/club-madeira-awin-connector/actions/runs/35457042170) **success** on `dc25deb` |
| Playwright / join invent | Absent |
| Secrets committed | No (`.env.example` placeholders only) |
| Build self-declared Join UAT | No (correct) |

### npm test summary

```
Test Files  5 passed (5)
     Tests  21 passed (21)
```

**Bob:** First ticket P0-P4 accepted. Ready for human Phase 0 / U-gate work. Not a Join ship.
